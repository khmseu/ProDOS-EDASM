{
    "name": "ts-edasm",
    "version": "0.1.0",
    "private": true,
    "type": "module",
    "description": "TypeScript reimplementation of the PRODOS Assembler Tools assembler (EDASM).",
    "license": "MIT",
    "scripts": {
        "build": "tsc -p .",
        "watch": "tsc -p . --watch"
    },
    "devDependencies": {
        "typescript": "^5.3.0"
    }
}